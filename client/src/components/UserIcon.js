import React from "react";
import UserMenu from "./UserMenu";

const UserIcon = () => {
  return (
    <div style={{ left: 1437, top: 14, position: "absolute" }}>
      <UserMenu />
    </div>
  );
};

export default UserIcon;
